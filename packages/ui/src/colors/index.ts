import { commonColors, darkCommonColors } from "./common";
import { semanticColors } from "./semantic";

const colors = {
  ...semanticColors,
};

export { colors, commonColors, darkCommonColors, semanticColors };
